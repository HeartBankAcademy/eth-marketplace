pragma solidity ^0.4.23;

// import "../lib/openzeppelin/ownership/Ownable.sol";
// import "../lib/openzeppelin/ownership/HasNoTokens.sol";

// /**
// * @title ProductStore
// * @dev Store contract implementation
// * @notice v1
// */
contract ProductStore { 
  
}
// contract ProductStoreV1 is HasNoTokens, Ownable {

  

//   /**
//    * @dev Allows the current owner to transfer control of the contract to a newOwner.
//    * @param _newOwner The address to transfer ownership to.
//    */
//   function transferOwnership(address _newOwner) public onlyOwner {
//     _transferOwnership(_newOwner);
//     if (address(ownershipDelegate) != address(0)) {

//     }
//   }
  

// }
